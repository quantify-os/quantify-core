.. admonition:: Examples
    :class: dropdown, tip

    .. jupyter-execute::
        :hide-code:
        :hide-output:

        from quantify.utilities._docs_helpers import create_tmp_dir_from_test_dataset
        import quantify.data.handling as dh

        tuid = "20210419-153127-883-fa4508"
        tmp_path, tmp_dir = create_tmp_dir_from_test_dataset(tuid=tuid)
        dh.set_datadir(tmp_path)

    .. jupyter-execute::

        import quantify.analysis.rabi_analysis as ra
        a_obj = ra.RabiAnalysis(tuid=tuid).run()
        a_obj.display_figs_mpl()

    .. jupyter-execute::

        a_obj.dataset

    .. jupyter-execute::

        a_obj.dataset_processed

    .. jupyter-execute::
        :hide-code:
        :hide-output:

        tmp_dir.cleanup()
